/**
 * Minecraft LE — Engine Stub
 * LEGAME Studio
 * Future home of the voxel rendering, chunk system, and game loop.
 */

export const Engine = {
  initialized: false,

  init(canvas) {
    console.log('[Engine] Voxel engine not yet implemented. Canvas ready for integration.');
    this.canvas = canvas;
    this.initialized = true;
  },

  start() {
    // Game loop placeholder
  },

  stop() {}
};
